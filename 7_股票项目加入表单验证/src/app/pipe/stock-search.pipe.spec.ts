import { StockSearchPipe } from './stock-search.pipe';

describe('StockSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StockSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
